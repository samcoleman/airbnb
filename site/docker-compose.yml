services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - portal-data:/app/data
    env_file:
      - .env

volumes:
  portal-data:
